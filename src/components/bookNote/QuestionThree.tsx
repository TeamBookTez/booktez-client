import { PreNoteLayout } from ".";

export default function QuestionThree() {
  return (
    <PreNoteLayout question="익명의 독서가(비회원) / OOO 독서가 님은 이 책에 어떤 기대를 하고 계신가요?">
      여기에 각자 넣을 내용들
    </PreNoteLayout>
  );
}
