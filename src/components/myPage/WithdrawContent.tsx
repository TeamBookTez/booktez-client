import styled from "styled-components";

export default function WithdrawContent() {
  return <StWithdrawText>WithdrawContent</StWithdrawText>;
}

const StWithdrawText = styled.p`
  position: absolute;
`;
